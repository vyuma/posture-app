{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "cat_overlay"],
  "permissions": [
    "core:default",
    "core:window:allow-set-fullscreen"
  ]
}
