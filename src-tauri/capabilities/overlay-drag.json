{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay-drag",
  "description": "Allow only the pet window to use native dragging",
  "windows": ["cat_overlay"],
  "permissions": ["core:window:allow-start-dragging"]
}
